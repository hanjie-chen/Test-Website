services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: my-website
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - ARTICLES_DIRECTORY=/articles-data
      - FLASK_APP=app.py
      - LOG_DIR=/var/log/personal-website
